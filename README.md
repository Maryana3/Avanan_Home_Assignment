## Installation
    `python3 -m venv venv`
    `source venv/bin/activate`
    `pip install -r requirements.txt`

## Start the project
    `source venv/bin/activate`
    `python main.py`

## Run with docker
    `docker-compose up`